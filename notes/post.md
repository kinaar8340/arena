# Post (three frames, not the six-page PDF)

Do not post the note as the PDF. Post these three frames in order.

1. `fig/formal-full-object.png` — Figure 1, the split.
2. `fig/fiber-diagram.png` — Hopf fiber: base catalog-visible, fiber the remainder.
3. `fig/s1-sweep.png` — Protocol S1, open-loop. \(r(\sigma,T)\) and \(A[p]\) vs \(\sigma\), mark \(\sigma_*\).

**Caption**

Model, not QGA spine. \(\sigma=0\) is pure transport; \(\sigma_*\) is where catalog jumps dominate.

**Link**

The PDF: `tex/main.pdf` (companion note, labeled Model).

S1 is open-loop: correction \(C\) is defined in the note and unused in the sweep.
No equation for \(Z\). No \(350/\pi\), no Magic Islands.
